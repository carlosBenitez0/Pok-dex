/* // const API_KEY = '4287ad07'

import { useSelectedFilter } from "./useSelectedFilter";

export const useSearchPokemonData = (search: string) => {
  const { filterPokemon } = useSelectedFilter();
  if (search === "") return null;
  filterPokemon(search);
  return { searchPokemonFunction };
};

export default useSearchPokemonData;
 */
